<Window x:Class="AniDiaryBooks.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Логин:" Margin="0,0,5,5" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="UsernameTextBox" Margin="0,0,0,5" />

        <Label Grid.Row="1" Grid.Column="0" Content="Пароль:" Margin="0,0,5,5" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="PasswordTextBox" Margin="0,0,0,5" />

        <Button Grid.Row="2" Grid.Column="1" Content="Войти" Click="LoginButton_Click" HorizontalAlignment="Right" Padding="10,5" Margin="0,10,0,0"/>

    </Grid>
</Window>